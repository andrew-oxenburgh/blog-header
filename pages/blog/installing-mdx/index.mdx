import BlogPost from '../../../components/BlogPost'

export const meta = {
    title: 'Starting my blog',
    description: 'How I got here',
    date: 'June 10, 2021',
    readTime: 5
}

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

# Installing

I picked up this [blog post](https://www.smashingmagazine.com/2020/09/build-blog-nextjs-mdx/) and went from there.

As is the case with a lot of tutorials, download from the sandbox and work backwards from there.

I then modified it by:
 - changing `post-n` to useful names.
 - pulling out css into jss - The CSS was inlined as style, and I prefer JSS, so...



